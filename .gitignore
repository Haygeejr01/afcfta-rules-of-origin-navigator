.venv/
__pycache__/
*.pyc
.pytest_cache/
logs/checkpoints/
logs/_smoke.json
logs/_*.log

# The real redaction anchors. NEVER commit this: an anchor is a verbatim copy of the
# sentence it removes, so publishing the rule list would republish, in one convenient
# place, exactly the content the redaction took out of the transcripts.
# eval/redaction_rules.example.json ships instead.
eval/redaction_rules.local.json

# Built from HEAD on demand; never a tracked artifact.
afcfta-navigator-submission.zip

# Raw OBS takes. The finished video lives on YouTube; the footage is working
# material and has no place in a public repo.
videos/

# Editor-local only. Currently holds a files.exclude block that tidies the
# explorer for the demo video; delete the folder once recording is done.
.vscode/


