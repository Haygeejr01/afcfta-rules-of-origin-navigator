# CHANGELOG — build log

Real entries from the build, in the order they happened. Every "Evidence" cell is
something observed in a run or a test, not a prediction.

## Build stages

| Stage | What I tried and why | Evidence | Decision / Learning |
|---|---|---|---|
| **Model selection** | Spec called for Qwen 2.5 7B *instruct*. Only the **coder** variant (`qwen2.5-coder:7b`) and a 1.5B coder were on disk. Started pulling `qwen2.5:7b`. | Pull ran at **387 KB/s, 5% after several minutes, ETA 3h11m** — and it would throttle the cloud model's bandwidth for the whole window. | **Killed the pull.** Ran everything on `qwen2.5-coder:7b`. A code-tuned model is a fine fit for the two nodes that matter most (strict-JSON extraction, pick-one-from-a-closed-list) and it turned out to write acceptable narrative prose too. Recorded as a real constraint rather than glossed as "we used Qwen 7B". |
| **Data before code** | Built `mock_tariff_db.json` and `synthetic_manifests.json` with full ground truth before writing a single node, so evaluation could never be tuned to fit the implementation. | 10 manifests, ground truth hand-computed. **All ten HS subheadings reviewed against HS-2022 and confirmed correct by hand before submission** — the dataset's real-code claim is checked, not merely asserted. | Held. All 10 HS subheadings are **real HS-2022 codes**; the **RVC thresholds are synthetic** and labelled as such in the dataset's own `provenance` field. Fabricating thresholds *and* presenting them as authoritative would be the one unrecoverable integrity error here. |
| **Ground truth needs a guard** | Hand-computed numbers rot the moment a manifest is edited. | — | Wrote `tests/test_ground_truth.py`: it recomputes every declared RVC% from the declared costs, re-derives every threshold from the rule table, and asserts each declared verdict against the real calculator. The dataset cannot silently drift from the code that grades against it. |
| **Money arithmetic** | Started with plain `float`. | With a ±2pp borderline band, verdicts hinge on tiny differences, and `4080/8000*100` is not exactly `51.0` in binary floating point. | Moved all sums and the division to `Decimal`, quantised once to 2dp. |
| **Rounding and verdicts must agree** | Realised a system could compute an unrounded 49.997, report "50.00%", and print "FAIL" beside it. | — | The **quantised** value is what gets compared to the threshold. A report reading "50.00% against a 50% threshold: FAIL" is now unreachable. Test: `test_reported_percentage_and_verdict_never_disagree`. |
| **Baseline fairness** (important) | The obvious baseline withholds the rule table and relies on the model's own knowledge. | Our thresholds are **synthetic** — no model could ever recover them. Withholding the table would guarantee a 0% baseline on every threshold. | **Pasted the full rule table into the baseline's single prompt.** Still one zero-shot call, no tools, no schema, no deterministic math. This makes the comparison about what the architecture adds, not about who memorised an invented number. Without this the whole evaluation would have been rigged. |
| **Baseline grading tolerance** | Needed a tolerance for RVC comparison. | Agent arithmetic is exact to 2dp. | Used **±0.5pp for both systems**. A tolerance can only ever help the baseline here, never the agent. |
| **Ran the baseline** | 10 manifests, one prompt each, temperature 0. | **HS code 9/10. RVC percent 0/10. Verdict 5/10.** Avg 37.8s, 1 call/case. | The finding I did **not** expect: the model is *good* at HS classification. Claiming classification as the architecture's win would have been false. |
| **Looked at *how* the baseline failed** | Read the raw responses instead of only the scores. | Every response was **three bare lines with no working**. M001: `RVC_PERCENT: 47.50%` (truth 79.41%). M008: `27.69%` (truth 67.32%). **And the decisive one: M002 and M010 came back byte-identical — `8544.42 / 37.00% / FAIL` — despite being a Moroccan wiring harness (true RVC 11.33%) and a South African solar module with a different component list, different origins and a different total cost.** | Asking for "two decimal places" produced *false precision*, not arithmetic. `27.69%` looks computed; it is fabricated. The duplicate pair proves it: one number for two unrelated bills of materials means the model was matching a plausible percentage to the **HS code**, not dividing one cost by another. This is the hot take (see README). |
| **Worst-case analysis, not just accuracy** | Checked the *direction* of each baseline error. | **M008: said FAIL, truth PASS** (67.32% vs a 30% threshold). **M004: said FAIL, truth PASS.** **M006: said PASS at 41.86%, truth 34.00% against a 35% threshold.** | Both error directions carry real cost: false negatives make an exporter abandon a valid preference claim; false positives lead to a rejected claim at the border. "5/10 verdicts correct" understates the damage — all 5 correct verdicts rest on a wrong number. |
| **Framework choice** | Considered LangGraph for the workflow. | Control flow is one straight line plus a single bounded loop back to `LOOKUP_RULE` on reviewer edit; the pause is a real process pause backed by a checkpoint file. | **Plain Python state machine.** A framework would add a dependency and an indirection layer without changing the control flow or the interrupt semantics. Revisit if the flow gains concurrent branches or needs distributed resumption. |
| **Retry / backoff** | Deliberately wrote the Ollama client with no retry, no backoff, no circuit breaker. | **10/10 baseline calls succeeded. 0 failures, 0 rate-limit responses, 0 unparseable JSON responses.** | **No retry code was added**, because nothing in evaluation justified it. If 429s or timeouts appear later, that is when the code changes — and this row is where the evidence would go. |
| **JSON parsing** | Considered a parse-and-retry loop for extraction. | Ollama's `format: json` constrains decoding at the grammar level. | No retry loop. Malformed JSON is *prevented*, not recovered from. Valid-JSON-but-wrong-shape still fails Pydantic and routes to human review, which is the correct destination for it. |
| **CLASSIFY_HS: prompt vs guarantee** | The prompt tells the model to pick only from the shortlist. | The baseline answered a **solar PV module with `8544.42`** — the adjacent electrical-conductor rule. Exactly the plausible-looking guess a prompt cannot prevent. | The node **validates the returned code against the rule table and overrides anything absent to `CLASSIFICATION_UNAVAILABLE`**. A prompt is a request; membership in the dataset is the only thing that counts. Confidence is also zeroed on override, since a rejected code says nothing about confidence in the final answer. **This did not fix the case that motivated it** — `8544.42` is *in* the table, so the guard passes it. See "The HS guard is narrower than I had claimed" below; I wrote this row believing otherwise. |
| **VERIFY produced noise on first live run** | First real M001 run flagged `China, Germany, Netherlands` as "unrecognised origins". | Those are correctly-identified foreign countries, not extraction errors. VERIFY exists to *save* reviewer attention and was spending it. | Split origin resolution into **eligible / external / unknown**. Only genuinely uninterpretable strings now raise a flag; recognised imports pass silently. Added `known_external_origins` to the data file and a regression test (`test_routine_imports_do_not_raise_a_review_flag`). **This node changed because of an observed problem, not a predicted one.** |
| **Origin alias layer justified itself** | Added fuzzy origin matching (aliases, "City, Country", embedded country names) as cheap defence. | The live extraction returned **"Ivory Coast"**, not the dataset's "Cote d'Ivoire". Without the alias map, M001's RVC would have silently dropped from 79.41% to 54.9% and still looked plausible. | Kept. A silently-wrong percentage is this product's worst failure mode, so defence at the origin boundary is worth its ~30 lines. |
| **GENERATE_REPORT: structure over instruction** | Could have asked the model to restate the figures accurately. | The baseline's whole failure mode is confident fabricated numbers. | The report's numeric fields are **assembled from state in Python**; the model writes only named prose fields. There is no code path from a model token to a reported figure. A regex scan additionally flags any figure it writes into prose, so "the model ignored the instruction" is recorded rather than assumed away. |
| **Cold start measured** | First call took ~25s for a 2-token reply. | Subsequent **baseline** calls 33–45s. | Cold model load is real and reported honestly in REPRODUCTION.md rather than hidden by warming the model before timing. |
| **Two eval runs raced each other** | Relaunched the comparison after the first run's task handle was lost mid-session, assuming it had died. | It had not. Both processes were alive and competing for the same local model. The orphan finished first and wrote a complete 10-row result file; **agent per-case latency climbed monotonically 105s → 263s across the run**, which is contention, not workload. | Killed the duplicate, preserved the contaminated file as `logs/comparison_results_contended.json`, and re-measured with nothing else running. **Accuracy figures were unaffected** (temperature 0, same prompts, deterministic tools) but the latency figures were worthless and would have been reported as real. Checking that a background process is actually dead is cheaper than the retraction. |
| **Cloud escalation NOT enabled** | Spec allows escalating to `qwen3.5:cloud` where the local model struggles. | Local model scored **9/10 on HS classification** and produced usable report prose. | **Not enabled.** No node demonstrated a need. The env var `NAVIGATOR_CLOUD_MODEL` makes escalation a one-line change if evidence later appears, but shipping it now would be complexity without a cause. |
| **1.5B model not used** | Available on disk. | No experiment gave a reason to introduce a third model. | Unused, and therefore not claimed as part of the architecture. |
| **Human checkpoint is structural** | Could have made approval a flag. | — | `GENERATE_REPORT` is unreachable except through an explicit `approve` decision, and state is checkpointed to disk before the pause so the interrupt survives process exit. The evaluation harness stops at `VERIFY` and does **not** simulate the human, so no accuracy number is credited to a scripted approval. |
| **`--list` was broken** | Documented `python src/graph.py --list` in REPRODUCTION.md, then ran it. | `error: one of the arguments --manifest --file --stdin is required` — argparse enforced the required source group before control ever reached the `--list` branch. | Made the source group optional and enforced the requirement manually *after* `--list` exits. Found only because I ran every command I had written down instead of trusting that I had written it correctly. |
| **Trajectory capture needed a reviewer** | The three demonstration logs each need someone to type at a blocking prompt, which makes them unreproducible by a reader and impossible to regenerate in CI. | — | Added `--decision` (plus `--edit-hs-code`). It renders the same panel, checkpoints the same state, and still reaches `GENERATE_REPORT` only through an explicit approve — but it writes `SCRIPTED DECISION ... This was not a human sign-off` into `human_notes`, so a log produced this way cannot be mistaken for one. **The interactive prompt stays the default.** A scripted reviewer still cannot introduce an HS code outside the dataset. |
| **Tested the no-invented-numbers claim instead of asserting it** | README and this file both claimed "no code path from a model token to a reported figure". That was true *by inspection*, which is not the same as true. Wrote `tests/test_report_integrity.py`: stub the model with a hostile narrative (invents 99.99% RVC, a 5% threshold, USD 1,000,000 of cost, and calls itself "a legally binding certificate of origin") and assert the assessment block is unmoved. | The isolation held — every figure still came from state, and the scan caught the fabricated ones. **But a parametrised case found a real bug:** a model returning `{"summary": null}` produced a report whose summary was the literal string `"None"`. `str(None)` is truthy, so the `narrative_ok` fallback never fired and `"None"` would have shipped as the summary of a real document. | Added a `_text()` coercion treating JSON null as absent. **The claim I was most confident about is the one where the test found a defect** — and it was in the null-handling around the guarantee, not the guarantee itself. Suite went 99 → 110 tests. |
| **Tested the checkpoint instead of trusting the control flow** | "GENERATE_REPORT is unreachable except through an approve" was a claim about an `if` statement I had written and read. Wrote `tests/test_checkpoint.py`: the whole workflow with the model stubbed by prompt shape and a scripted reviewer, asserting `reject`, a bare `edit`, and an exhausted edit loop all terminate with the report node never running. | All paths held. The edit loop is genuinely bounded at `MAX_EDIT_ROUNDS` and an edit produces a **second** `CALCULATE_RVC` trace event rather than relabelling the first. | Kept. The tests also pin the prompt shapes: the stub dispatches on prompt content and raises on anything unrecognised, so a prompt that stops asking for the shape the nodes parse now fails a test instead of failing in production. Suite 110 → 119. |
| **Documented a command that taught the wrong lesson** | Wrote the trajectory-capture commands into REPRODUCTION.md, including `M010 --decision edit --edit-hs-code 8544.42`. | M010 is a solar PV module (HS 8541.43, deliberately absent). `8544.42` is the adjacent electrical rule — **the exact wrong guess the baseline made** and the one the classification guard exists to catch. | Changed M010's demonstration to `--decision reject`, which is the correct reviewer action when no rule in the controlled set covers the product. Writing the reproduction steps carefully is what surfaced this; the code was right and the documentation was teaching people to defeat it. |
| **The HS guard is narrower than I had claimed** (most important finding) | README and this file said "the LLM cannot invent an HS code". Ran the agent on M010 expecting `CLASSIFICATION_UNAVAILABLE`. | **The agent returned `8544.42 / 12.31% / FAIL`.** The guard checks *membership in the rule table*, and `8544.42` **is** in the table — it is the adjacent insulated-conductor rule. The model proposed a plausible in-dataset neighbour and the guard passed it straight through. The baseline made the identical error. **And `VERIFY` raised no warning:** the trajectory log records a self-reported confidence of **1.00** on the wrong rule, and the low-confidence trigger fires below 0.50. **Nothing automated in the system flagged this case.** | **Rewrote the claim rather than the code.** A membership check bounds the *vocabulary* of answers, not their correctness; a self-reported confidence is not a calibrated probability — and it turned out not even to be a variable (see the row below). What actually catches M010 is the human reviewer, who sees "solar photovoltaic module" filed under "insulated electrical conductors". This is the one case in the evaluation where the human step is load-bearing rather than ceremonial — and I only know that because the case was designed to have no right answer available. A description-vs-product plausibility check would be the next thing to try, but it needs its own evidence before it earns a model call. |
| **Deterministic arithmetic is only as good as the extraction** | Agent scored 9/10 on RVC. Checked the miss. | **M007: agent 89.02% vs ground truth 87.80%.** The calculation was *correct* — on a bill of materials where one component cost had been mis-read. Status still landed on PASS, so the verdict survived; the number did not. | Recorded, not patched. Moving arithmetic into a pure function eliminates arithmetic error; it does not eliminate *input* error, and claiming otherwise would repeat the mistake in the row above. The honest framing: the workflow converts a fabrication problem into a transcription problem, which is narrower, checkable by a human against a costing sheet, and visible in the BOM printed on the review panel. |
| **The confidence score is a constant, so the guard built on it is dead** | Before submitting I went back through the trajectory logs to find the actual confidence figure behind M010, rather than repeat the README's inference from the threshold in the source. | **The model self-reported `1.00` on M010's wrong rule.** Widening the search to every record that logs the field — 4 trajectories plus their 4 checkpoints, 9 records including a synthetic one — the only values that have **ever** appeared are `1.00` from the model and `0.0` written by our own override code. Eight correct classifications and one wrong one all scored identically. | `LOW_CONFIDENCE_THRESHOLD = 0.5` therefore fires **only** on the case the membership guard has already caught, and can never fire on a confident wrong answer — which is the only case where it would be worth having. A warning path that cannot trigger when it matters is not a safeguard, it is decoration. **Left in place and relabelled rather than deleted:** the panel prints it as "model self-reported", and a reviewer seeing `1.00` beside "solar module → insulated conductors" learns something true about the model that a hidden field would not teach them. Two lessons: an inference from a constant in the source is not a measurement, and this is the second time a claim I was confident about turned out narrower than written — both times found by reading the artifacts instead of the code. |
| **The timing note was true of the baseline and quietly wrong about the workflow** | Before submitting I dumped per-node durations out of all six trajectory logs, instead of re-reading the sentence I had written about them. | REPRODUCTION.md said calls "ran 33–45 s for real work" — measured on the **baseline**, which makes one call. The workflow's actual per-call range is **`EXTRACT` 68–169 s, `CLASSIFY_HS` 14–82 s, `GENERATE_REPORT` 69–88 s**, and a full approved run is **158–339 s**, not the ~95 s the note implied. **Same node, same machine, same prompt, 2.5× spread** — `CLASSIFY_HS` at 14 s on one run and 82 s on another. | The reported **97.7 s/case is correct and stays**: it measures the *graded* 2-call pipeline that stops at the checkpoint, which is what the comparison grades. What was wrong was the surrounding prose, which invited a reader to expect that number from an interactive run that makes three calls. Replaced with a measured table of ranges, an explicit "budget 3–5 minutes", and the spread stated as spread rather than averaged away. The variance is memory pressure on a laptop, not workload — which is the same root cause as the contended eval run above, and the second time latency is the one thing this project measured carelessly. Accuracy figures were never affected; they are temperature-0 and deterministic downstream. |
| **The report claimed legal clearance, three lines above the disclaimer saying it had none** (worst defect found) | Ran the full approved workflow on M001 end to end and actually read the document it produced, instead of checking that it produced one. | The narrative model wrote: **"You can now export your chocolate bars without any issues related to trade regulations."** Directly above a disclaimer reading "not a legal determination of origin, not a certificate of origin, and carries no authority". The prompt already forbade exactly this — `src/nodes.py` says *"Do NOT claim the product is legally certified, approved, or cleared by any authority"* — and the model wrote it anyway. Worse, the `limitations` field, the one field that would have counterbalanced it, was **generated on every run since the node was written and never rendered**: present in the prompt, present in the JSON, dropped on the floor by `render_report`. | Added `_scan_for_overclaims`, the exact structural twin of the figure scan: a phrase list of affirmative assurances, a negation guard so *"this is **not** a certificate of origin"* stays legal, hits surfaced as `claims_flagged_in_prose` in the metadata **and** the trace, node status downgraded to `warn`, and a warning block rendered **above** the summary rather than beneath it — a caveat printed after the claim is not a caveat. The offending wording is left visible rather than silently scrubbed, so the reviewer sees what the model tried to say. `limitations` now renders. **Then I ran the fix against a live model on a different product to see whether it worked on output it had never seen.** M008 (soap) produced four overclaims in one narrative — *"found to comply with the regulations"*, *"you can export them without any issues"*, *"you can continue exporting"*, *"meet the necessary trade compliance requirements"* — and the first version of the scan caught **one of the four**. So the list was widened on that evidence, and both real narratives are pinned as test fixtures, which keeps it growing against observed output rather than imagined output. Ten tests. Suite 119 → 129. **The lesson is the same one for the third time, and by now it is the actual finding:** every claim in this project that turned out narrower than written was found by reading an artifact — a log, a raw response, a rendered document — and none of them by reading code. Prompt-level prohibitions are requests; only structure and inspection are guarantees. Two honest limits, and the M008 run is the proof of the first: this is a phrase list, so like the HS membership guard it bounds *recognised phrasings*, not meaning; and `GENERATE_REPORT` runs **after** the human checkpoint, so the reviewer approves the figures and never sees the prose — the scan stands in for a second review rather than replacing one. **Both logs are committed** — `logs/trajectories/M001-20260829-142355.json` is the defect, `M008-20260829-151947.json` is the verification, and because the latter was written before the patterns were widened it still records `1` flag where the current code finds `4`. The gap is checkable in one command; REPRODUCTION §8 has it. |
| **Ran the comparison a second time, to find out which of my numbers were reproducible** | Every figure in the README came from a single 10-case run. A single run cannot distinguish "the architecture works" from "that sample went well", and reproducibility is weighted second in the judging criteria. So I re-ran the whole comparison with `--reuse-baseline` into a separate file, deliberately **not** overwriting `logs/comparison_results.json` — the file every published figure cites. | **Accuracy is bit-identical.** 9/9/9/8 both times, and all **ten per-case result payloads match byte for byte**, not merely in score. **Latency is not:** mean agent wall-clock went **97.7 s → 198.3 s**, a uniform **1.9–2.6×** across all ten cases with no case reordering. | Two conclusions, and they point opposite ways. The accuracy claims are **measured as reproducible**, which is what `temperature=0` plus grammar-constrained JSON plus deterministic arithmetic is supposed to buy and had until now only been asserted. But **"2.6× the latency" was a claim I had to narrow**: the ratio came from one machine state, and a uniform 2× swing across every case means the second decimal place is not defensible. README now prints the run-2 column beside run 1 and states the latency figure as an order of magnitude. **The third latency mis-measurement in this log** — after the contended eval run and the per-node prose — and by now the pattern is not carelessness, it is that on a laptop under memory pressure wall-clock is simply not a stable measurement, and I should have been reporting it as a range from the first run. Kept as `logs/comparison_results_run2.json`. |
| **The submission had no coding-agent trace at all, which is a qualification gate rather than a scoring one** | The operator logged in and pasted the gated pages. Reading them against the repo instead of against my memory of the public page. | Three statements stack: *"Coding-agent use is required. You must disclose the tools you used and submit the required trajectories for evaluation"*; *"representative trajectories for **every agent you used**, easy to follow from the agent instructions through to the final result"*; and a qualification gate that scores a submission *"only after it passes eligibility, completeness, integrity, **trace** and reproducibility checks."* **The repo disclosed the coding agent nowhere** — no README mention, no `CLAUDE.md`, and both Claude Code transcripts sitting outside the repo. A submission failing the trace check is **not scored**, so this outranked the variance work I was in the middle of. | Wrote `eval/digest_agent_session.py`, a stdlib-only renderer that turns a raw Claude Code JSONL transcript into a readable trajectory — **no model summarises anything in it**, which is the same rule this project applies to numbers, and the raw `.jsonl` ships beside each digest so any line can be checked against source. Committed both sessions: **18 human instructions, 16.9 h, 456 tool calls, 12 failures, 3 human interrupts, 14 checkpoints.** The failures are the point — one of them, `No task found with ID: bxc5v2joh`, is the artifact behind *"Two eval runs raced each other"* eight rows above, so the trace independently corroborates a defect this log already confessed. **Four counting bugs in the renderer, all found by reading its output against the raw file**, and they are worth listing because the same mistake keeps recurring in this project: `<task-notification>` entries and `[Request interrupted by user]` were counted as instructions; the **original role-and-spec prompt was being truncated**, which is the single most load-bearing instruction in the submission; any message *containing* `<ide_opened_file>` was filed as machine-generated, but the IDE **appends** that to genuine messages, so more than half the operator's instructions vanished into "editor state"; and the harness's own `"Continue from where you left off."` (`isMeta: true`) plus a queued message it re-recorded under two uuids were both being counted. The published figure moved 16 → 9 → 18 → **16** before it stopped moving, and it is a coincidence that the first wrong number and the right one are the same. Each is now reproducible from the `.jsonl` in one command, which is the only reason I trust the fourth one. `logs/agent_sessions/README.md` maps each rubric sub-requirement to where it is answered, and distinguishes the **three** agents involved — the coding agent that built this, the workflow agent inside it, and the baseline agent it is measured against — because all three produce files called "trajectories" and conflating them would misrepresent the evidence. |
| **Publishing the transcripts meant publishing things that were nobody's business** | Reading my own trace as a judge would, before pushing it. Two days of messages to a coding agent are not written for an audience, and this repository is public. | The transcripts carried the operator's private remarks about how they felt about the outcome, their account handle on the host platform, a gated contest page pasted for reference, and a scored review of this project they had obtained elsewhere and pasted in for context. None of it shaped an engineering step; all of it would have been permanently public. The tempting fix was the wrong one — **rewriting the prompts to read like a professional brief.** That would have been a fabricated trace submitted under a rules check that names integrity, and detectable anyway, since the surrounding tool calls and timestamps do not move when the prose does. | Wrote `eval/redact_agent_session.py`: a declared list of anchors, **73 redactions across 41 entries**, each leaving a visible `[redacted: category]` marker, with the categories and counts tabulated in `logs/agent_sessions/README.md`. **It redacts and does not rewrite** — no message reworded, no instruction invented, nothing technical removed, and specifically the agent's own criterion-by-criterion scoring of this project left in place, unflattering parts included, because deleting critical feedback to look better is the same defect in a different coat. The tool fails closed: non-zero if a rule matches nothing, if a redacted string survives anywhere, if any independent probe string survives, or if the entry count or JSON validity changed. **It leaked five times before it passed. The fourth was the tool itself, and the fifth had already been pushed.** First version touched only the operator's own messages, and copies survived in the agent's replies and in tool output. Second, a span rule needed both anchors in one string, and truncated context summaries carried the opening anchor alone. Third, the compaction summaries **paraphrase** earlier messages, so an exact-anchor rule matched the original and missed the restatement — and the rules-survived check passed all three times, because asking "is each rule's own anchor gone" is circular. The probe list, which is independent of the rules, is what caught them, so it ships with the tool. **The fourth was different in kind and much more embarrassing:** a rule's anchor is a verbatim copy of the sentence it removes, so `eval/redact_agent_session.py` — with the anchors declared inline — was a tidy list of exactly the content it had just taken out of the transcripts, about to be committed to a public repository. The secret scan over the files going public caught it, matching the operator's account handle. Anchors and probes moved to `eval/redaction_rules.local.json`, which is gitignored; `redaction_rules.example.json` ships with the same structure and placeholder anchors, and the counts are published instead of the strings. **The fifth is the one that got out.** Every rule matched, every probe passed, and the commit was pushed — but the 12 probes were all anchored on the *openings* of the removed sentences, and a compaction summary had quoted one from the *middle*. Four fragments of a private message shipped in `d1080d2` and were caught only when the operator asked, plainly, whether the content was still there. The probe list is now 26 strings and covers interior phrases; the history was rewritten rather than patched, because a follow-up commit leaves the old blob readable in `git log -p` forever. **The lesson is the one this log keeps recording:** the tool that enforces a property is not exempt from it, a passing check is only as wide as what it thought to look for, and every one of these was found by scanning the output rather than reasoning about the design. One consequence worth stating: re-snapshotting the transcript would need a fresh redaction pass, because each compaction restates every earlier message in new wording — the shipped snapshots verify clean, the still-growing source file does not. |

## What changed reactively vs. what was designed up front

**Designed up front:** deterministic arithmetic, deterministic lookup, typed state with
`extra="forbid"`, the `CLASSIFICATION_UNAVAILABLE` sentinel, the human interrupt.

**Changed because a run showed a problem:**
- VERIFY's origin flagging (noise on first live run → three-way classification)
- Confidence zeroing on HS override (a rejected code was carrying forward a confidence score that described a different answer)
- `_step_summary` printing a total cost that isn't populated yet at EXTRACT time
- `--list` crashed on argparse's required-source group — found by running a command I had already written into REPRODUCTION.md
- A JSON `null` narrative field shipping the literal string `"None"` as a report summary — found by the hostile-narrative test, not by reading the code
- The narrative asserting legal clearance, and the `limitations` field never being rendered — both found by reading a real report end to end, not by reading `render_report`

**Deliberately not built:** retry/backoff (no failures observed), LangGraph (no branching
need), cloud escalation (local model sufficient), the 1.5B model (no experiment justified
it), cryptographic signing (explicitly out of scope — the output is a draft assessment,
not a certificate).
